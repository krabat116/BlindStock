# DEPRECATED — Azure App Service 배포 워크플로우
# Fly.io로 마이그레이션 완료 (deploy.yml 참조)
# 이 파일은 비활성화 상태입니다.

name: "[DEPRECATED] Deploy Backend to Azure App Service"

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "이 워크플로우는 더 이상 사용하지 않습니다."
        required: true

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use deploy.yml (Fly.io) instead."
